📁 Assumed Project Structure
backend-pythonhackthon/
├── app/
│   └── main.py
├── requirements.txt
└── Dockerfile

use this below coomands
app.py is diif and main.py is diff and dockerfile for app.py and main.py is diferent



commands below:
source venv/bin/activate
pip install "pydantic[email]"
uvicorn app.main:app --host 0.0.0.0 --port 8000
pip install -r requirements.txt

